var x, y

if(ball.left && BotPlayer.user){
    if(ball.down){
        x = ball.x - (canvas - ball.y)
        y = canvas.height - ball.height
    }else if(ball.up){
        x = ball.x - 
    }
    if(x >= user.x + user.width){

    }
}